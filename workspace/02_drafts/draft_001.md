---
id: draft_001
type: draft
status: revised
revision_count: 2
source: design_002
output_type: long_article
created_at: 2026-05-30
updated_at: 2026-06-01
---

# AIエージェントだけじゃない――外部API連携のトークン管理基盤として見るAuth0 Token Vault

## 成果物タイプ

output_type: long_article

## 設計書との対応確認

| 設計書の要素 | 本文での反映箇所 |
|---|---|
| もっとも伝えたいこと | 第3章 3-1、第6章 6-4 |
| 読者の到達状態 | 第4章 4-5、第6章 6-1〜6-3 |
| 持ち帰り 1（つらさの中心はトークンのライフサイクル管理） | 第1章 1-3 |
| 持ち帰り 2（自作の責務を棚卸しすると価値を測れる） | 第4章 4-2、4-3 |
| 持ち帰り 3（基盤に寄せても認可設計は残る） | 第6章 6-2 |

---

## 本文ドラフト

> 読み方の階層: この記事は「章（大項目）→ 節（中項目）→ 節内の小見出し（小項目）」の3階層で構成しています。各章の冒頭に、その章で扱う節の一覧を置きます。読みたい場所から読めます。

## 第1章 この記事の主張とスコープ

この章で置くことは2つです。

- 主張: Token VaultはAIエージェント専用の機能ではない
- スコープ: 「外部API連携のつらさ」のうち、どこに絞って話すか

この章の節は次のとおりです。

- 1-1. 導入：Token VaultはAIエージェントだけのものか
- 1-2. 今回扱う「外部API連携のつらさ」のスコープを絞る
- 1-3. ユーザー代理で外部APIを呼ぶときに必要なこと

### 1-1. 導入：Token VaultはAIエージェントだけのものか

Token Vaultは、AIエージェント専用の機能ではありません。外部API連携を持つアプリケーション全般に価値があります。

#### 観測：入り口はAIエージェントに置かれがち

Auth0のToken Vaultは、Auth0 for AI Agentsの文脈で紹介されることが多い機能です。AIエージェントがユーザーの代わりに外部サービスを操作する。その実現手段として登場します。

公開されている記事やドキュメントの多くも、AIエージェントを入り口にToken Vaultを説明しています。例として次のものがあります（URLと要約は著者が最終確認する想定です）。

- Auth0公式「Token Vault for AI Agents」: https://auth0.com/ai/docs/intro/token-vault
  - AIエージェント向け機能としてToken Vaultを位置づけた入門ドキュメント。
- Auth0公式ドキュメント「Token Vault」: https://auth0.com/docs/secure/call-apis-on-users-behalf/token-vault
  - 外部サービスのトークンを保存・管理し、ユーザーの代わりにAPIを呼ぶための機能としての説明。
- Auth0ブログ「Auth0 Token Vault: Secure Token Exchange for AI Agents」: https://auth0.com/blog/auth0-token-vault-secure-token-exchange-for-ai-agents/
  - タイトルの時点でAIエージェント文脈での紹介になっている。
- Zenn（waddy氏）「Auth0 for AI Agents を理解するために Google カレンダーと連携するサンプルアプリを動かす」: https://zenn.dev/waddy/articles/start-genkit-auth0-for-genai
  - AIエージェントのサンプルアプリでToken Vaultを有効化する手順を扱う。
- DevelopersIO「Auth0 for AI AgentsのToken Vaultが、『何トークンを保管するのか』『保管するだけなのか』検証・考察してみた」: https://dev.classmethod.jp/articles/auth0-for-ai-agents-token-vault/
  - これもAI Agents文脈での検証記事。

これらを見ると、Token Vaultの入り口はAIエージェントに置かれることが多い、と言えます。

#### 動機：触れてみて、限定は適切でないと感じた

AIエージェントとの相性が良いことは認めます。ただ、価値はそこに限定されるのか。実際にToken Vaultに触れてみて、AIエージェントに限定して考えるのは適切ではない、と感じました。価値はもっと広いはずです。それを伝えたい。これがこの記事を書いた動機です。

#### この記事の進め方

この記事では次の順序で話します。

1. Token Vaultが何を楽にしてくれるか
2. 外部トークン管理を自作するなら何を考える必要があり、その重さをToken Vaultがどう肩代わりしているか
3. AIエージェント以外でどう使えるか
4. 現時点でできることと、今後期待したいこと

記事の軸を先に置きます。サードパーティAPIのトークン管理を、アプリ側で抱え込まない。Auth0に安全に任せる。この軸で見ると、Token Vaultの価値はAIエージェントより広く見えてきます。

#### 「自作するなら」の位置づけ

2番目の「自作するなら」について補足します。これは自作をすすめる話ではありません。自作したときに必要になることを数えると、Token Vaultが肩代わりしている責務の大きさが見えます。その代替の大きさは、想像より大きいです。それを実感してもらうための観点です。記事の中心は、あくまでToken Vaultの価値と可能性です。

### 1-2. 今回扱う「外部API連携のつらさ」のスコープを絞る

今回は「ユーザーの代理で外部APIを呼ぶための、トークンの取得・保存・更新・利用」だけに絞ります。

#### 外部API連携のつらさは広い

外部API連携のつらさは広い範囲に及びます。例として次のものがあります。

- ログ設計
- ネットワーク制御
- レート制限
- 監査
- 障害対応
- API仕様変更への追従

これらをすべて扱うと話が散ります。

#### 絞る範囲と、絞る理由

今回はそこまで広げません。「ユーザーの代理で外部APIを呼ぶための、トークンの取得・保存・更新・利用」に絞ります。

絞る理由は、Token Vaultが効くのがまさにこの範囲だからです。Token Vaultは、外部サービスのaccess token / refresh tokenの取得・保存・更新・利用を、アプリの外側（Auth0側）に出せます。この記事で見る「つらさ」は、Token Vaultがアプリの外に出してくれる範囲とちょうど重なります。だからこの範囲に絞ります。

### 1-3. ユーザー代理で外部APIを呼ぶときに必要なこと

つらさは「API実行処理」そのものではなく、「実行するためのトークンのライフサイクル管理」にあります。

#### 具体例：ユーザーの代わりに外部APIを叩く

次のような機能を考えます。

- 自分のアプリからユーザーのGoogle Calendarを読む
- ユーザーの代わりにSlackへ投稿する
- ユーザーのGitHubリポジトリ情報を取得する

#### 実現に必要なこと

これらは、単にAPIを叩けば終わりではありません。実現するには、次のことが必要になります。

- 外部サービス連携の許可を得る
- Auth0ユーザーと外部アカウントを紐づける
- access token / refresh tokenを取得する
- それらを安全に保存する
- access tokenの期限切れに対応する
- refresh tokenで更新する
- scope不足や再同意に対応する

#### つらさの本体はトークンのライフサイクル管理

つまり、つらさは「API実行処理」そのものにはありません。「実行するためのトークンのライフサイクル管理」にあります。ここがこの記事の論点です。外部API連携を作るとき、見るべきはトークンの取得・保存・更新・利用です。

## 第2章 Token Vaultが効く領域と、その仕組み

この章では、Token Vaultが何に効くのかと、どう動くのかを確認します。

この章の節は次のとおりです。

- 2-1. Token Vaultはこのトークン管理の領域に効く
- 2-2. Token Vaultの概要：紐づけと取得の流れ
- 2-3. ドキュメント上の代表例：AIエージェント

### 2-1. Token Vaultはこのトークン管理の領域に効く

Token Vaultが効く範囲を列挙します。

- 外部サービスのトークンの保存・管理・更新
- Auth0ユーザーとの紐づけ
- 外部API用access tokenの取得

Auth0のドキュメントでは、Token Vaultは外部サービスのトークンを安全に保存・管理するサービスとして説明されています。Connected Accountsの仕組みで外部アカウントを紐づけます。その結果、外部サービスのaccess token / refresh tokenをToken Vaultに保存できます。

> 出典: Auth0ドキュメント Token Vault概要（メモ内URLを本文化時に明示）

### 2-2. Token Vaultの概要：紐づけと取得の流れ

ここでは、紐づけの構造と、アプリがトークンを取得する流れを分けて書きます。

#### 基本構造：誰が何に紐づくか

基本構造は次のとおりです。

```
Auth0ユーザー
  └─ Connected Account
       └─ External Provider（Google / Slack / GitHub / Microsoft / Salesforce など）
```

連携が完了すると、Auth0が外部プロバイダーのaccess token / refresh tokenをToken Vaultに保存します。

#### アプリがトークンを取得する流れ

アプリ側の取得の流れは次のとおりです。

1. アプリはAuth0のaccess token / refresh tokenを持つ
2. それを使ってToken Vaultに問い合わせる
3. 外部API用のaccess tokenを取得する

#### 誤解しやすい点：魔法の変換ではない

ここで誤解しやすい点を正確に書きます。Auth0のトークンが、何もないところから外部トークンへ変換されるわけではありません。事前にユーザーが連携へ同意しています。その結果、外部トークンがToken Vaultに保存されています。アプリはそれを安全に取り出しているだけです。この理解が正確です。

### 2-3. ドキュメント上の代表例：AIエージェント

#### 代表例で起きること

代表例としてAIエージェントの文脈を見ます。AIエージェントがユーザーの代わりに動きます。Google Calendarを読む。Slackへ投稿する。GitHubやSalesforceを参照する。

このとき、AIエージェント自身にrefresh tokenやAPIキーのような長期権限を直接持たせたくありません。エージェントが長期権限を抱えるのは避けたい状況です。

#### Token Vaultがもたらす形

Token Vaultを使うと、保存と更新はAuth0側に寄せられます。エージェントは必要なときだけ、短命な外部API用access tokenを取得します。長期権限を持たない設計にできます。

AIエージェントとの相性が良いことは率直に認めます。否定はしません。その上で、次章で「でもそれだけではない」へつなげます。

## 第3章 主張：AIエージェントだけの話ではない

この章は短い章です。記事の核となる主張を1つ置きます。

この章の節は次のとおりです。

- 3-1. 今日いちばん話したいこと

### 3-1. 今日いちばん話したいこと

ここが記事の核です。

#### 本質は「ユーザーに紐づく外部トークン管理基盤」

Token Vaultの本質は、外部サービスのOAuthトークン管理を、Auth0ユーザーに紐づけて安全に扱える点にあります。トークンの保存・更新・取得を、ユーザー単位で安全に扱う基盤です。

#### だから価値はAIエージェントに限らない

だとすれば、価値はAIエージェントに限りません。外部API連携を持つアプリケーション全般に価値があるはずです。これが私の主張です。

#### この価値を見る2つの観点

この価値を、2つの観点で見ます。

- 観点1: 自作するとどれだけ大変か（第4章）
- 観点2: AIエージェント以外でどう使えるか（第5章）

## 第4章 観点1：なぜ「Auth0に任せられる」のが嬉しいのか

この章は、自作の責務を数えることでToken Vaultの価値を測ります。自作をすすめる章ではありません。

この章の節は次のとおりです。

- 4-1. 自作の話は、主題ではなく価値を測るために使う
- 4-2. 自作すると抱える責務を短く棚卸しする
- 4-3. 特に重い責務を3つに絞って説明する
- 4-4. AWSで作れなくはないが、記事の主題にはしない
- 4-5. ここをAuth0に寄せられるのが嬉しい

### 4-1. 自作の話は、主題ではなく価値を測るために使う

先に線を引きます。この章は「自作Token Vaultを作ろう」という話ではありません。

この記事の軸を再確認します。サードパーティAPIのトークン管理を、アプリ側で抱え込まない。Auth0に安全に任せる。この価値と可能性を見ることが目的です。

その価値を具体的に見せる方法として、自作した場合に出てくる責務を棚卸しします。自作構成の詳細に寄りすぎません。「どの責務をAuth0側に寄せられるのか」を見せる補助線として扱います。

### 4-2. 自作すると抱える責務を短く棚卸しする

#### 自作で必要になる責務の一覧

自作すると、次の責務が必要になります。

- 連携開始・callback用のエンドポイント
- プロバイダーごとのOAuth処理
- ユーザーと外部アカウントの紐づけ管理
- トークンの安全な保存
- トークン取得・交換用のエンドポイント
- refreshによる更新処理
- 監査ログ・運用監視

各項目はここでは深掘りしません。目的は、Token Vaultに寄せられる責務の広さを見せることです。

#### この一覧の使い方

この一覧は点検に使えます。自分のアプリで既に抱えている責務はどれか。これから抱えそうな責務はどれか。外部APIを1つ足すだけのつもりでも、裏側ではこれだけの責務が増えます。

### 4-3. 特に重い責務を3つに絞って説明する

棚卸しした責務のうち、重いものを3つに絞ります。

#### 責務1: 連携開始からcallbackまでのOAuthフロー

OAuthフローでは次の処理が必要です。

- provider判定
- scope決定
- 認可画面へのredirect
- state検証
- token交換

プロバイダーごとに差があります。1つ作るだけでも手数が多い領域です。

#### 責務2: トークンの安全な保存と更新

refresh tokenは、外部APIへの継続アクセス権そのものです。だから扱いが重くなります。

- 暗号化して保存する
- ログに残さないよう抑止する
- 期限切れ時に更新する
- 失効時に再同意へ誘導する

保存して終わりではありません。更新と失効への対応まで含みます。

#### 責務3: ユーザー・外部アカウント・scopeの紐づけ

1人のユーザーが複数のGoogleアカウントを連携することがあります。このとき、どの連携のトークンを返すかを管理する必要があります。ユーザー・外部アカウント・scopeの対応を保持します。

#### 3つを並べて分かること

トークン保存だけの話に見えます。実際には、OAuthフロー、保存、更新、紐づけまで抱えます。

### 4-4. AWSで作れなくはないが、記事の主題にはしない

#### 構成の候補

AWSで構成するなら、次のサービスが候補になります。

- API Gateway
- Lambda / ECS
- DynamoDB / RDS
- KMS
- Secrets Manager
- CloudWatch Logs

#### 持ち帰ってほしい判断

ただし、本文ではアーキテクチャ解説に広げません。読者に持ち帰ってほしいのは、構成の詳細ではありません。「作れなくはないが、作ると責務が多い」という判断です。

この記事は技術記事プラットフォームへの投稿を想定します。図解を入れる場合も1枚程度にします。自作構成を主役にしません。

### 4-5. ここをAuth0に寄せられるのが嬉しい

#### 寄せられる責務の再掲

自作で出てきた重い責務を再掲します。

- OAuth連携フロー
- provider差分
- token保存
- token更新
- Auth0ユーザーとの紐づけ
- 再同意対応
- 安全なtoken管理

これをAuth0側に寄せられます。ここがToken Vaultの魅力です。

#### 寄せた後のアプリの形

アプリ側は、外部トークンの保存・更新を自前で抱えません。必要なときにToken Vaultから取得します。この形にできます。

#### 次章への橋渡し

ここで次章へつなげます。外部トークン管理の責務は、機能やサービスをまたいで共通です。だとすれば、AIエージェント以外でも価値が出ます。

## 第5章 観点2：AIエージェント以外での活用

この章では、AIエージェント以外の具体的な活用例を2つ置きます。

この章の節は次のとおりです。

- 5-1. 外部API連携の共通基盤として考える
- 5-2. マイクロサービス構成での利用イメージ

### 5-1. 外部API連携の共通基盤として考える

Token Vaultは、本質的にはユーザーに紐づいた外部トークン管理の基盤です。だからAIエージェント以外でも使えるはずです。

#### 1つのSaaS内に連携が増える例

1つのSaaS内の例を置きます。

- 通知機能 → Slack API
- カレンダー機能 → Google Calendar API
- ファイル連携 → Google Drive / Microsoft
- 開発連携 → GitHub API
- バックエンドジョブ → 各種外部API

#### 個別管理だと責務が散る

各機能が個別にトークン管理を持つと、どうなるか。保存・更新・再同意導線の実装が、複数箇所に散ります。同じセキュリティ責務も複数箇所に散ります。

#### 共通基盤に寄せた形

Token Vaultに寄せると、連携状態をAuth0ユーザーに紐づけて管理できます。各機能は必要なときに取得するだけです。共通基盤として扱えます。

### 5-2. マイクロサービス構成での利用イメージ

#### 各サービスがトークンを持つと分散する

マイクロサービス構成を考えます。各サービスがrefresh tokenを直接持つと、責務が分散します。

- サービスA: Google連携
- サービスB: Slack連携
- サービスC: GitHub連携

トークン管理が各サービスに散ります。

#### Token Vaultを中心に置く形

Token Vaultを中心に置く構成が考えられます。各サービスは外部トークンを長期保存しません。必要なときに、ユーザーに紐づくaccess tokenを取得します。

#### 注意：全サービスに開けてはいけない

ここで注意を入れます。すべてのサービスが自由にToken Vaultへアクセスできる設計は危険です。次の点は別途設計が必要です。

- どのサービスがアクセスできるか
- どのユーザーの連携にアクセスできるか
- どの連携に、どの範囲でアクセスできるか

便利だからと全サービスにアクセスを開けてはいけません。

## 第6章 懸念と期待

この章では、現状の価値・残る設計・今後への期待を分けて書きます。混ぜると、期待が現状機能に見えてしまうためです。

この章の節は次のとおりです。

- 6-1. 現時点でできること：トークン管理をAuth0に寄せる
- 6-2. ただし、権限設計は残る
- 6-3. 今後期待したいこと：実行時の最小権限化
- 6-4. まとめ

### 6-1. 現時点でできること：トークン管理をAuth0に寄せる

ここは要望ではありません。現状の価値です。Token Vaultで現時点で言えることを整理します。

- 外部サービスのトークンを、Auth0ユーザーに紐づけて保存・管理できる
- アプリ側がrefresh tokenを直接保存し続ける責務を減らせる
- 外部API用access tokenを、必要なときに取得する形に寄せられる

refresh tokenをアプリ側で直接持たなくてよい。このメリットは現時点でも大きいです。

### 6-2. ただし、権限設計は残る

Token Vaultを使っても、すべてが安全になるわけではありません。認可設計は残ります。

#### 残る設計

次の設計は残ります。

- どのサービスにToken Vaultアクセスを許可するか
- どのユーザーの外部連携にアクセスできるか
- どのprovider・どのscopeを許可するか
- 業務上、その外部API操作を許可してよいか

#### 具体例

具体例を置きます。

- あるサービスはGoogle Calendar読み取りだけ
- 別のサービスはSlack投稿用だけ
- あるサービスにはGitHubアクセスを与えない

Token Vaultは、トークン管理の重さを減らします。ただし、認可設計を不要にはしません。基盤は責務を減らしますが、設計を消しません。

### 6-3. 今後期待したいこと：実行時の最小権限化

ここからは現状説明ではありません。今後への期待として書きます。

#### 連携時のscopeと実行時の必要権限はずれる

連携時に許可したscopeと、実行時に必要な最小権限の粒度は、一致しないことがあります。

具体例を置きます。

- 連携時はCalendar read/writeを許可した
- でも今回の処理はreadだけでよい
- 別の処理ではwriteが必要

#### 期待する体験

実行時のコンテキストに応じて、外部APIに渡す権限をより細かく絞れる。そういう体験を期待します。

- 今回はreadだけ渡す
- この処理ではwriteだけ渡す
- このサービスにはSlackのみ渡す
- このサービスにはGitHubを渡さない

#### 現状の価値と期待の関係

現時点でも、refresh tokenをアプリ側で直接持たなくてよいメリットは大きいです。その上で、実行時の最小権限化が扱いやすくなると、AIエージェント以外の用途でも、より安心して使えます。

### 6-4. まとめ

#### 主張の再掲

Token VaultはAIエージェントだけのものではありません。これを再掲します。

#### 魅力を3点で整理

魅力を3点で整理します。

1. 外部トークン管理をAuth0に寄せられる
2. 自前だと重い責務を減らせる
3. 外部API連携の共通基盤として使える可能性がある

#### 注意と期待を分けて整理

注意と期待を分けます。

- 注意: 権限設計は残る
- 期待: 実行時の最小権限化が扱いやすくなるとさらに使いやすい

最後に。実行時の最小権限化が強化されれば、AIエージェント以外でも、Token Vaultはより魅力的になります。

---

## 著者が加筆すべき箇所

### 加筆ポイント 1

該当箇所: 1-1. 導入 / 2-1. Token Vaultはこのトークン管理の領域に効く
足すとよさそうな内容:
- 1-1 に参照記事・ドキュメントのURLを5件追加した。著者がこの中から残す・足すを選ぶ。特に著者自身の記事（web.maronn-room.com の Token Vault デモ解説）を入り口に置くか検討する。
- 各URLの要約は最小限にしてある。著者の実体験（どこで・どの登壇で見て「AIエージェント文脈で紹介されがち」と感じたか）を1〜2行足すと、観測が体感として立つ。
- 2-1 で引用するAuth0ドキュメントの一文（原文）を著者が確認して引く。
なぜ読者価値が上がるか:
- 「紹介されがち」という観測を読者が追える形になった。著者の実体験が加わると、一般論ではなく著者固有の引っかかりとして読める。

### 加筆ポイント 2

該当箇所: 2-2. Token Vaultの概要：紐づけと取得の流れ
足すとよさそうな内容:
- 実際のConnected Accountsの連携画面、またはアプリからaccess tokenを取得するコード片
- 「魔法のように変換されるわけではない」を読者が誤読しないための1ステップ図
なぜ読者価値が上がるか:
- 流れを言葉だけで読むと、同意ステップが抜け落ちて理解されやすい。具体物があると誤解を防げる。

### 加筆ポイント 3

該当箇所: 6-3. 今後期待したいこと：実行時の最小権限化
足すとよさそうな内容:
- 著者が「ここがこうなると嬉しい」と感じた実体験・困った場面
- 現状のToken Vaultで実行時にscopeをどこまで制御できるかの事実確認（できる/できないの現状線引き）
なぜ読者価値が上がるか:
- 期待だけだと要望に見える。現状の事実と並べると、読者が導入判断に使える情報になる。

## AIが自信を持てていない箇所

- 2-1 / 2-2 のToken Vaultの仕組み記述が、Auth0の最新仕様と一致しているか。Connected Accountsとの関係、保存対象（access/refresh token）の正確さは著者の確認が必要。
- 6-1 / 6-3 で「現時点でできること」と「今後期待したいこと」の境界を引いたが、その境界が実際のToken Vault仕様と合っているか。実行時のscope制御が現状どこまで可能かは確証がない。
- 1-1 に追加した参照URLは、検索で見つけた実在ページだが、内容が最新版か、著者の意図する引用先かは未確認。著者が開いて取捨選択する前提で置いている。
- Token Vaultが RFC 8693（OAuth 2.0 Token Exchange）ベースである点は2-2の理解と整合するが、本文には踏み込んで書いていない。出典として足すかは著者判断。

## 読者価値のチェック

### 読者の認識が変わる箇所

- 第1章 1-3: 外部API連携のつらさの中心が「API実行」ではなく「トークンのライフサイクル管理」だと切り分けられる。
- 第3章 3-1: Token VaultをAIエージェント用機能ではなく、外部API連携の共通基盤として捉え直せる。

### 読者の行動や判断が変わる可能性がある箇所

- 第4章 4-2 / 4-3: 自作時の責務を棚卸しすることで、マネージド機能の価値を自分のアプリ文脈で測れる。
- 第6章 6-2: 基盤に寄せても認可設計は残ると理解し、導入時に「設計が消えた」と誤解せず計画できる。

### まだ弱い箇所

- 発表スライドが元のため、著者の語り口（〜と思っています等）の熱量をどこまで残すか未確定。本文は説明寄りに整えたため、著者固有の温度が薄まっている可能性がある。1-1 の動機部分は、著者の引っかかり（「価値はそこに限定されるのか」「触れてみて適切ではないと感じた」）を残す方向で維持している。
- 今回のRevision 2は構造化（見出し階層と章冒頭の節一覧）が中心で、文章そのものの温度は1-1以外ほぼ変えていない。構造を整えたことで説明が平板に寄った箇所がないか、著者の確認がほしい。

## 成果物タイプとしての違和感

- long_articleとして、章ごとの分量バランスはおおむね設計書どおり。第4章が観点1として5節あるが、設計書の方針（自作論を主役にしない）に沿って各節を短くしたため、章全体が過剰に長くはなっていないと判断する。
- 各節を小見出し（####）で分割したため、節が短い章（第3章）では小見出しが細かく見える可能性がある。技術記事プラットフォームの見出し表示で過剰にならないか、著者レビューで確認したい。

## 設計書との差分（変えた点があれば）

- 章・節の構成・順序は変更していない。設計書 design_002 の第1章〜第6章、各節の見出しと役割をそのまま保持している。
- 【表現上の変更・目次は不変】Revision 2 で、各節の内部を「####」レベルの小見出しに分割し、各章の冒頭に「その章で扱う節の一覧」を追加した。これは設計書の「節内で書くこと」を、読者がつかみやすい大→中→小の階層に整理し直したものである。節内トピックの内容自体は追加・削除していない。見出しレベルは「章=##／節=###／節内の小見出し=####」に統一した。
- 【要・著者判断・継続】1-1 の「話す順序」リストの2番目を、設計書の文言「なぜ自作と比べて嬉しいか」から「自作するなら何を考える必要があり、その重さをToken Vaultがどう肩代わりしているか」へ変更している（Revision 1 から継続）。著者のレビュー意図に従った変更だが、設計書（design_002）の1-1記述とは表現がずれる。設計書側も同じ方針に更新してよいか、著者の判断を仰ぎたい。
- 1-1 冒頭に章の役割を示す短い導入文、および参照URLリストを追加している（Revision 1 から継続）。いずれも節の役割・順序は変えていない加筆である。

---

# Review Log

## Review 1

review_status: needs_revision
reviewed_at: 2026-05-31

（注: 人間レビューは本文中にインラインの「(RC: ...)」コメントとして記入された。下記はその内容を整理したもの。原文は本文から該当箇所に付されていた。）

### 修正してほしい点

1. 第1章「主張とスコープ」の章の意図と書き方が不明瞭。主題は「Token VaultはAIエージェントに限定せず価値がある」ことなので、スコープ章の置き方が分かりにくい。
2. 1-1：「AIエージェントの文脈で紹介されがち」の根拠として、Zennなどの技術記事・ドキュメントの参照URLと概要をサンプルとして示してほしい。
3. 1-1：動機の文章がAIっぽい。「Token Vaultに触れてみて、AIエージェントに限定して考えるのは適切ではないと感じた。それを伝えたいのが記事を書いた動機」というニュアンスにしてほしい。
4. 1-1：「話す順序」の2番目「なぜ自作と比べて嬉しいのか」が本筋とずれている。自作したいのではなく、自作するならここまで考える必要があり、その重さをToken Vaultが肩代わりしている。想像より大変なことを代替していると実感してもらいたい。
5. 1-2：「先に方向性を示す」と書いているのに具体的な方向を書いていない。文章も生成AIっぽい。

### 次回レビューで確認すること

- 第1章の意図と読み口が明確になったか。
- 参照URLが観測の裏付けとして機能しているか。
- 動機と「自作」の位置づけが、著者の意図どおりに伝わるか。

## レビュー二回目
`wakariyasusa-review`スキルを使い文章構造を大項目→中項目→小項目と情報の構造がつかみやすいように全体的に行って。

---

# Revision Log

## Revision 1

revised_at: 2026-05-31
based_on_review: Review 1（本文中インライン (RC: ...) コメント）

### 対応した修正

1. 【指摘1】第1章冒頭に、章の役割を示す導入文を追加した。「この章では主張（AIエージェント専用ではない）と扱う範囲の限定を先に置く」と明示し、スコープ章の意図を分かるようにした。
2. 【指摘2】1-1 に参照URLを5件追加した（Auth0公式ドキュメント2件、Auth0ブログ1件、Zenn記事1件、DevelopersIO記事1件）。各URLに1行の概要を付け、「AIエージェント文脈で紹介されがち」という観測を読者が追える形にした。著者の取捨選択・自身の記事追加を前提に、加筆ポイント1へ誘導した。
3. 【指摘3】1-1 の動機部分を書き直した。「私はここに引っかかりました」を削り、「実際にToken Vaultに触れてみて、AIエージェントに限定して考えるのは適切ではないと感じた。それを伝えたい」という指摘どおりのニュアンスにした。著者の引っかかり（「価値はそこに限定されるのか」）は残した。
4. 【指摘4】1-1「話す順序」の2番目を「外部トークン管理を自作するなら何を考える必要があり、その重さをToken Vaultがどう肩代わりしているか」に変更した。さらにリスト後に補足を追加し、自作をすすめる話ではないこと、代替の大きさが想像より大きいこと、それを実感するための観点であることを明示した。
5. 【指摘5】1-2 に具体的な方向を追記した。「Token Vaultが効くのは、外部のaccess/refresh tokenの取得・保存・更新・利用をアプリの外（Auth0側）に出せる範囲であり、この記事で見るつらさとちょうど重なる」と書き、抽象的な予告で終わらせないようにした。

### 対応できていない修正

- 指摘2の参照URLについて、各記事の中身が著者の意図する引用先か、最新版かまでは確認していない。
- 著者自身の実体験（どの登壇・場面で「紹介されがち」と感じたか）の追記は、著者しか書けないため加筆ポイントとして残した。

### 対応できていない理由

- URLは検索で見つけた実在ページを置いたが、内容の最終的な取捨選択は著者の判断に属するため。
- 実体験は著者固有の情報であり、AIが代わりに書くと熱量が偽物になるため。

### 再レビューで見てほしい点

- 第1章の導入文とスコープ章の置き方が、読み口として自然になったか。指摘1が「章構成そのものを変えてほしい」という意味なら、設計書の目次変更になるため別途approvalが必要。今回は文章の明確化で対応した。
- 1-1「話す順序」2番目の文言を設計書の文言から変更した（設計書との差分に明記）。設計書側も同じ方針に更新してよいか。
- 追加した参照URLのうち、どれを残し、どれを著者自身の記事に差し替えるか。

## Revision 2

revised_at: 2026-06-01
based_on_review: レビュー二回目

### 対応した修正

1. `wakariyasusa-review`スキルの観点（ルール05全体地図／10優先順位／12相互関係／13視覚特性／15受信順序）に沿って、本文全体を「章（大項目）→ 節（中項目）→ 節内の小見出し（小項目）」の3階層に再構成した。
2. 見出しレベルを統一した。章=「##」、節=「###」、節内の小見出し=「####」とし、階層が見た目でつかめるようにした。従来は章が「#」、節が「##」で、節内に階層がない箇所が多かった。
3. 本文冒頭に「読み方の階層」を示す注記を置き、3階層構成であることと、読みたい場所から読めることを明示した（全体地図／受信順序）。
4. 各章の冒頭に「その章で扱う節の一覧」を追加した。読者が章の現在地と中項目の並びを先に把握できるようにした（全体地図→現在地）。
5. 各節の先頭に、節の結論を1文で置いた（1-1・1-2・1-3・5-1・6-2 など）。流し読みでも各節の主張をつかめるようにした（優先順位）。
6. 節内のべた書きを、意味のまとまりごとに「####」小見出し＋箇条書きに整理した（視覚特性／相互関係）。例：2-2を「基本構造／取得の流れ／誤解しやすい点」に、4-3を責務1〜3に、6-2を「残る設計／具体例」に分割した。

### 対応できていない修正

- 文章そのものの温度（著者の語り口）は、1-1以外は今回ほぼ変えていない。今回の指摘が構造化であるため、内容・文体の改変は最小限にとどめた。

### 対応できていない理由

- レビュー二回目の指摘は「情報構造のつかみやすさ」であり、構造化に集中するため。文体の温度調整は別観点であり、勝手に踏み込むと著者の熱量を変えてしまうため見送った。

### 再レビューで見てほしい点

- 「章=##／節=###／節内=####」の3階層が、技術記事プラットフォームの見出し表示で過剰に細かく見えないか。特に節が短い第3章で小見出しが細かすぎないか。
- 各章冒頭の「節の一覧」が、冗長な繰り返しになっていないか。目次として機能しているか。
- 各節先頭の「結論を1文」が、後続の本文と重複して見えないか。
