# TODO

## コンテンツ

- 移行した記事に `published_at` を入れる（現状 `totp-in-ci-with-okta` 以外は公開日が未設定で、
  日付が表示されず一覧の並びも slug 順になっている）
- `articles/about-ciba-and-exec-auth0.md` は `published: false` のまま。仕上げて公開するか判断する

## サイト

- OGP 画像の生成
- サイトマップの出力
- 本文まで含めた検索（現在はタイトル・トピック・抜粋のみが対象）
- 画像の最適化（現在は元ファイルをそのまま配信している）

## 運用

- セキュリティチェック
- `main` ブランチの保護設定の確認（投稿権限 = push 権限のため）
